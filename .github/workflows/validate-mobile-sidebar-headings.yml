name: Validate vertical mobile sidebar headings

on:
  push:
    branches:
      - agent/mobile-sidebar-headings
    paths:
      - .github/apply-mobile-sidebar-headings.py
      - .github/workflows/validate-mobile-sidebar-headings.yml

permissions:
  contents: write

jobs:
  apply-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: agent/mobile-sidebar-headings
          fetch-depth: 0

      - name: Apply exact mobile sidebar changes
        run: python3 .github/apply-mobile-sidebar-headings.py

      - name: Check diff formatting
        run: git diff --check

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Commit verified billing workspace
        run: |
          git config user.name "Telenexus Admin"
          git config user.email "admin@telenexustechnologies.com"
          git add frontend/src/pages/BillingWorkspace.jsx
          if ! git diff --cached --quiet; then
            git commit -m "Restore vertical mobile sidebar headings"
            git push origin HEAD:agent/mobile-sidebar-headings
          fi
