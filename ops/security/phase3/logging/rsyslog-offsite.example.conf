# Configure only after a TLS syslog collector has been provisioned.
# Replace the hostname and CA path, then install as /etc/rsyslog.d/60-nexa-offsite.conf.
# Never forward over unauthenticated UDP.
#
# global(DefaultNetstreamDriver="gtls")
# global(DefaultNetstreamDriverCAFile="/etc/ssl/certs/offsite-syslog-ca.pem")
# action(
#   type="omfwd"
#   target="logs.example.net"
#   port="6514"
#   protocol="tcp"
#   StreamDriver="gtls"
#   StreamDriverMode="1"
#   StreamDriverAuthMode="x509/name"
#   StreamDriverPermittedPeers="logs.example.net"
#   queue.type="LinkedList"
#   queue.filename="nexa_offsite"
#   action.resumeRetryCount="-1"
# )
